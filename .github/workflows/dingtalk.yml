name: dingtalk

on:
  watch:
    types: [started]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: dingtalk robot message
        uses: fifsky/dingtalk-action@master
        with:
          url: https://oapi.dingtalk.com/robot/send?access_token=d6eb1bc323f43017f33727f197f392ce5e793e5c7003f6c39234bc67fc01f42e
          type: markdown
          content: |
            # 错的是五花八门，非常奇葩的都有。
