name: dingtalk

on:
  watch:
    types: [started]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: dingtalk robot message
        uses: fifsky/dingtalk-action@master
        with:
          url: ${{ secrets.DINGTALK_WEBHOOK}}
          type: markdown
          content: |
            # 发布频道推送
            ## 推箱子大作战 v1.4 Release 正式版现在发布！
            ### 本次发布改进了下载方式，只需下载一个文件即可
            ### 锤子不是万能的，没有锤子是万万不能的
            ![Getting-Over-It](https://raw.githubusercontent.com/Galaxy-Studio-Team/sources/master/Getting-Over-It/icon.png)
            ### [点击此处立即体验](https://galaxy-studio.ga/2020/05/16/GettingOverIt)
            > 星际工作室智能推送
